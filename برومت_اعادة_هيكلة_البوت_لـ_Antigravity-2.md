# برومت شامل — إعادة هيكلة بوت "study_test_bot" إلى حزمة ملفات مقسّمة

> **كيفية الاستخدام:** انسخ هذا الملف كاملاً كأول رسالة/تعليمات نظام في Antigravity،
> وأرفق معه ملف `study_test_bot.py` الحالي (نسخة واحدة ~7400 سطر) كمرجع تنفيذي وحيد
> لكل الميزات. لا تحتاج أي ملف آخر — هذا المستند مكتفٍ ذاتياً بكل قرار ومواصفة.

---

## 1) نظرة عامة على النظام

هذا بوت تيليجرام لإدارة جامعة (طلاب تقنية معلومات في اليمن)، اسمه الداخلي
"بوت الجامعة". النظام الكامل يتكوّن من 4 بوتات منفصلة:

- `study_test_bot.py` — **البوت المستهدف بهذه المهمة فقط**. بوت الطلاب/الأدمن/المالك
  الرئيسي: محاضرات، تكاليف، ملخصات، ملازم، نماذج اختبارات، مساعد ذكاء اصطناعي،
  إدارة مستخدمين، بث إشعارات.
- `contact_bot.py`, `stealth_bot.py`, `log_bot.py` — **خارج النطاق تماماً.** لا تُنشئ
  لها ملفات، لا تُعدّل عليها، لا تفترض بنيتها. البوت الحالي يتواصل مع `log_bot.py`
  فقط عبر HTTP داخلي (راجع القسم 9) — هذا الاتصال **يبقى كما هو حرفياً بلا تعديل**.

**التقنيات:** Python 3، `pyTelegramBotAPI` (telebot) 4.32.0+، `gspread` (Google Sheets
كقاعدة بيانات كاملة، لا قاعدة بيانات أخرى)، `pytz` (توقيت اليمن Asia/Aden UTC+3)،
نشر على Render (Free tier). المكتبات: راجع `requirements.txt` المرفق مع الملف الأصلي
إن وُجد (telebot, gspread, google-auth, oauth2client, pytz, requests, flask,
firebase-admin, gunicorn, python-dotenv).

> ⚠️ **ملاحظة مؤكَّدة:** `firebase-admin` موجود بـ `requirements.txt` لكنه **غير
> مستخدَم إطلاقاً بأي مكان بالكود الفعلي** (تحقّقنا: صفر إشارة لكلمة "firebase" بكل
> الملف الأصلي). تجاهله تماماً، **لا** تُنشئ أي ملف أو منطق متعلق بـ Firebase — قاعدة
> البيانات الوحيدة والحصرية هي Google Sheets عبر `gspread`.

**التخاطب:** كل النصوص، الأزرار، وأسماء المفاتيح داخل الكود **بالعربية** (متغيرات
ودوال بأسماء عربية أحياناً) — هذا مقصود ويجب الحفاظ عليه، لا تُترجم أسماء الدوال أو
المفاتيح إلى الإنجليزية.

---

## 1.1) ملفات جاهزة مرفقة — ابنِ عليها ولا تُعِد كتابتها من الصفر

مرفق مع هذا البرومت أرشيف `study_bot_partial.zip` يحوي **18 ملفاً منجزاً ومُتحقَّقاً
منه فعلياً** (فحص syntax كامل + تشغيل استيراد فعلي متبادل بين كل الملفات + اختبار
سلوك حي لعمليات الحفظ) من نفس هيكلة القسم 6 أدناه:

```
config.py, logging_utils.py,
sheets/connection.py, sheets/cache_utils.py, sheets/users_repo.py,
sheets/data_repo.py, sheets/texts_repo.py,
utils/parsing.py, utils/time_parsing.py,
ai/providers.py, ai/assistant.py,
keyboards/main_kb.py, keyboards/flow_kb.py,
features/browsing.py, features/lectures.py, features/assignments.py,
features/summaries.py, features/booklets.py
```

**استخدمها كما هي** (لا تُعِد صياغتها إلا إذا وجدت خطأ فعلياً)، وأكمل عليها فقط باقي
الملفات المذكورة بشجرة القسم 6 (`sheets/rooms_repo.py` أو دمجها بـ `browsing.py` كما
هي الآن، `sheets/help_repo.py`, `ai/voice.py`, `features/onboarding.py`,
`features/settings.py`, `features/broadcast.py`, `features/users_admin.py`,
`features/help_materials.py`, `features/exams.py` (مع القرار [16])،
`features/upload_request.py`, `features/admin_commands.py` (3 أوامر فقط، القرار
4.5)، `files_io.py`, `state.py`, `http_server.py`, `bot.py`.

**خلل واحد وجدناه وأصلحناه أثناء التحقق، كُن منتبهاً لتكراره بأي ملف مشابه تكتبه
لاحقاً:** الدوال `save_lecture`/`save_task`/`save_summary`/`save_booklet` كانت تستدعي
`notify_auto_publish` (إشعار المشتركين بالنشر التلقائي) **داخل نفس try/except الخاص
بعملية الحفظ نفسها**. لو فشل استدعاء الإشعار لأي سبب (حتى مجرد كون `features/broadcast.py`
غير مبني بعد)، كانت الدالة تُرجع `False` وتُبلّغ الأدمن "❌ حدث خطأ" **رغم أن الصف
كان قد أُضيف/عُدِّل فعلياً بالشيت بنجاح** — تأكّدنا من هذا باختبار فعلي. الإصلاح
المطبَّق بالملفات المرفقة: عزل كل استدعاء إشعار بدالة مستقلة خاصة بها try/except
منفصل (`_notify_new_lecture`, `_notify_new_task`, `_notify_new_summary`,
`_notify_new_booklet`)، بحيث فشل الإشعار لا يؤثر أبداً على نتيجة الحفظ المُرجَعة
للمستخدم. **طبّق نفس المبدأ** بأي دالة حفظ أخرى تبنيها (مثل `save_exam` بملف
`features/exams.py` القادم) — أي إشعار/تنبيه جانبي يجب أن يكون بمعزل تام (دالة
مستقلة بـ try/except خاص بها) عن نجاح/فشل عملية الحفظ الأساسية.



---

## 2) المهمة المطلوبة منك بالضبط

حوّل ملف `study_test_bot.py` المرفق (ملف واحد ضخم) إلى حزمة بايثون مقسّمة اسمها
`study_bot/`، بحيث:

1. **كل ميزة/موضوع في ملف واحد مسؤول عنه فقط** — لا تكرار لنفس المنطق في ملفين.
2. **لا حذف لأي ميزة** إلا الميزات المذكورة صراحة في القسم 4 (قرارات الحذف) أدناه —
   وهي كلها قرارات نهائية معتمدة، نفّذها مباشرة بلا سؤال.
3. **لا تغيير في السلوك الظاهر للمستخدم** لأي ميزة غير مذكورة بقسم 4، حتى لو بدت لك
   الشيفرة قابلة للتحسين — فقط انقلها لمكانها الجديد بنفس المنطق تماماً.
4. اتبع هيكلة الملفات المحددة بالقسم 6 حرفياً (نفس الأسماء والمسارات).
5. اتبع سياسة الكاش الموضحة بالقسم 8 بلا استثناء.
6. أنتج شجرة ملفات كاملة قابلة للتشغيل مباشرة (تحل محل `study_test_bot.py` الحالي
   بالكامل، بنفس نقطة الدخول النهائية `run()`/`bot.infinity_polling()`).
7. عند الانتهاء، أعطني: (أ) شجرة الملفات الكاملة، (ب) ملخص بكل قرار حذف نفّذته
   ولماذا، (ج) أي نقطة غامضة اضطررت لاتخاذ قرار فيها بنفسك مع تبرير واضح.

---

## 3) قواعد عمل صارمة (لا استثناء)

1. **اقرأ ملف `study_test_bot.py` المرفق فعلياً وبالكامل قبل كتابة أي سطر** — لا تعتمد
   على وصفي النصي وحده لأي دالة، الوصف هنا للتوجيه والتأكد من عدم النسيان فقط، أما
   السلوك الدقيق (رسائل الخطأ، ترتيب الشروط، النصوص الافتراضية) فمصدره الوحيد الكود
   الفعلي المرفق.
2. **مبدأ "ميزة واحدة = ملف واحد."** إن احتجت نفس المنطق في مكانين، اجعله دالة
   واحدة في الملف المسؤول عنها، واستوردها من الآخر — لا تُعِد كتابتها.
3. **سياسة الكاش ثابتة ولا تُخرق (تفصيل كامل بالقسم 8):** أي قراءة من الشيت تُخزَّن
   بكاش لا يُحدَّث بمهلة زمنية (TTL) إطلاقاً. التحديث فقط بعد كتابة ناجحة على الشيت،
   أو أمر يدوي صريح. عند فشل أي تحديث كاش، **يبقى الكاش القديم دون حذف**، ويُرسل
   تنبيه لبوت اللوج (نفس آلية `LOG_BOT_TOKEN` الموجودة بالكود الأصلي).
4. **ممنوع أي حلقة استطلاع دورية (polling loop) جديدة** تقرأ الشيت كل X ثانية —
   باستثناء واحد صريح موجود أصلاً بالكود الأصلي ويجب الإبقاء عليه كما هو حرفياً:
   `_watch_sheet_loop` (فحص كل 30 ثانية لاكتشاف تعديل يدوي مباشر على شيت
   المستخدمين من خارج البوت، وإرسال إشعارات ترقية/تخفيض/تفعيل تلقائياً). هذه الحلقة
   غرضها مختلف عن كاش القراءة العادي (كشف تغيير خارجي، لا تحديث كاش الأداء)، فهي
   مستثناة من قاعدة "ممنوع Polling" ويجب نقلها كما هي.
5. لا تحذف أو "تنظّف" أي دالة تبدو لك غير مستخدمة إلا إذا كانت مذكورة صراحة بالقسم 4.
   إن وجدت شيفرة ميتة غير مذكورة هناك، انقلها كما هي مع تعليق `# ⚠️ يبدو كوداً غير
   مستخدَم — لم يُحذف احتياطاً` بدل حذفها.
6. حافظ على **كل** النصوص العربية الافتراضية، رسائل الأخطاء، الرموز التعبيرية
   (إيموجي) بالحروف والترتيب نفسه تماماً — هذه واجهة مستخدم حقيقية لطلاب حقيقيين.
7. لا تُضف أي مكتبة أو تبعية جديدة غير موجودة أصلاً بالكود المرفق.

---

## 4) قرارات نهائية معتمدة — نفّذها مباشرة بلا سؤال

هذه القرارات اتُّخذت مسبقاً بعد نقاش مطوّل، هدفها الأساسي **تسريع البوت وتقليل
التعقيد** دون أي أثر على تجربة الطالب. نفّذها كما هي بالضبط:

### 4.1 — تقليص مزوّدي الذكاء الاصطناعي إلى اثنين فقط
احذف بالكامل كل الدوال والمسارات الخاصة بمزوّدي: **NVIDIA، Anthropic، Cohere،
DeepSeek، Mistral، Groq** (الدوال `call_nvidia`, `call_anthropic`, `call_cohere`,
`call_deepseek`, `call_mistral`, `call_groq`, وكل الفروع الخاصة بها داخل
`load_ai_providers` و `ask_ai`). **يبقى فقط:** `call_gemini` و `call_openrouter`.
شيت `ai_providers` الفعلي المستخدم حالياً لا يحوي أصلاً إلا Gemini وOpenRouter، فهذا
الحذف لا يفقد أي وظيفة حية، فقط يزيل شيفرة ميتة تُثقل الملف وتُبطئ قراءته/صيانته.

### 4.2 — حذف كامل لميزة "استخراج الجدول من نص حر" (البطاقة الذكية)

**ملاحظة تنظيف بالملف المرفق `sheets/texts_repo.py`:** لا يزال بداخله 3 مفاتيح
افتراضية ميتة (`زر_sched_accept`, `زر_sched_edit`, `زر_sched_cancel`) تعود لهذه
الميزة المحذوفة — وجودها غير ضار حالياً (لا شيء يستدعيها بعد الحذف)، لكن احذفها من
`DEFAULT_BOT_TEXTS` عند الصياغة النهائية تحقيقاً للاتساق الكامل مع هذا القرار.

احذف بالكامل: `_EXTRACT_PROMPT`, `extract_schedule_from_text`,
`_format_schedule_card`, `_schedule_card_markup`, `_schedule_cards`,
`_temp_admin_actions` وكل الأزرار/الـ callbacks المرتبطة بها
(`sched_accept_*`, `sched_edit_*`, `sched_cancel_*`, وحالة `editing_schedule`
داخل معالج الرسائل). هذه الميزة كانت تسمح للأدمن بإرسال نص حر يحاول الذكاء
الاصطناعي تحويله تلقائياً لمحاضرات — تُستهلك فيها استدعاءات AI كثيرة وتُبطئ
الاستجابة، والإضافة اليدوية بالأزرار (➕ إضافة ← إضافة محاضرة) أسرع وأدق وتُغطي نفس
الحاجة تماماً.

**استثناء صريح مهم:** الدوال `_time12_to_24` و `normalize_time` و `parse_time_range`
**تبقى** رغم أنها كانت تُستخدم أيضاً بميزة الاستخراج المحذوفة، لأنها **أيضاً** تُستخدم
لتحليل الوقت اليدوي عند اختيار "⏰ توقيت آخر" أثناء إضافة/تعديل محاضرة (ميزة مستقلة
مطلوبة وتبقى). انقلها إلى ملف مستقل خاص بتحليل الوقت.

### 4.3 — حذف زر "🕐 إضافة محاضرة" المختصر من القائمة الرئيسية
لا يوجد اختصار مباشر بالقائمة الرئيسية لإضافة محاضرة. الوصول الوحيد المتبقي:
➕ إضافة ← إضافة محاضرة (نفس تدفق بقية أنواع البيانات، لتوحيد وتبسيط بنية القوائم).

### 4.4 — حذف أمرَي `/ai` و `/lang` (أوامر تيليجرام)
كلاهما بالكود الأصلي لا يفعل شيئاً غير رسالة "استخدم زر الإعدادات" — عديما الفائدة،
احذفهما. **لا علاقة لهذا بزر "⚙️ الإعدادات" نفسه (لوحة Inline لتفعيل مساعد نايف/النشر
التلقائي/اللغة) — تلك اللوحة تبقى كاملة كما هي.**

### 4.5 — تقليص الأوامر النصية الإدارية الحرة إلى 3 فقط
دالة `try_execute_admin_command` بالكود الأصلي تفهم عشرات الأنماط النصية (regex)
لإضافة/حذف محاضرات وتكاليف وملخصات وأسعار وتنبيهات نصياً، إضافة مستخدم نصياً،
استدعاء ملف بكلمة مفتاحية، كشف عناوين مرقمة متعددة (`confirm_multi`/`edit_multi`/
`reject_multi`)، وإعادة تعيين AI نصياً. **احذف كل هذه الأنماط بالكامل، وأبقِ فقط
الأنماط الثلاثة التالية** (بنفس الـ regex ونفس الرسائل المرجعة تماماً كما بالكود
الأصلي لهذه الثلاثة تحديداً):
   - إرسال إشعار (لمستخدم محدد بالـ ID، أو للجميع)
   - تفعيل/تعطيل AI لمستخدم (`فعّل/عطّل AI للمستخدم [ID]`)
   - تغيير رتبة مستخدم (`اجعل [ID] أدمن/مستخدم`)

كل ما عدا هذه الثلاثة محذوف: `confirm_multi_*`/`edit_multi_*`/`reject_multi_*`
والـ callbacks الخاصة بهما، إضافة/حذف محاضرة أو تكليف أو ملخص أو سعر أو تنبيه
نصياً، إضافة مستخدم نصياً بأمر حر، استدعاء ملف بكلمة مفتاحية عبر نص، "أعد تعيين AI"
كأمر نصي حر (تبقى فقط كدالة داخلية `ai_reset_model()` بلا مستدعي نصي حر بعد الآن).

أيضاً: نص المساعد الذكي التعريفي (الجزء الذي يشرح للـ AI "الأوامر المتاحة عبر
المحادثة" داخل `get_data_summary_for_ai`/`ask_ai`) يجب تحديثه ليطابق هذه الثلاثة
فقط، لا القائمة الموسّعة القديمة.

### 4.6 — حذف نظام التقاط الملفات عبر كلمة "نايف" في الـ caption
الكود الأصلي يحوي ميزة: إذا أرسل أي مستخدم مسموح له ملفاً وكتب كلمة "نايف" في
الـ caption، يُرسَل الـ `file_id` لبوت اللوج ويُحفظ بقاموس `_naif_files` محلياً
لاستدعائه لاحقاً بأمر نصي. بما أن جهة الاسترجاع النصية محذوفة أصلاً بالقرار 4.5،
احذف هذا النظام بالكامل من `handle_file` (الشرط `if "نايف" in caption`) والقاموس
`_naif_files` وأي إشارة له.

### 4.7 — حذف الكود الميت التالي (مؤكَّد أنه غير مستخدَم بأي مكان بالملف الأصلي)
- `search_users_by_name` و `normalize_name` — لا يوجد أي استدعاء لهما؛ البحث الفعلي
  يتم عبر `_smart_search_user` فقط. احذفهما، وأبقِ فقط `_smart_search_user`.
- `build_multiselect_kb` وأزرار "تحديد الكل/تم التحديد" (`ms_subj:`/`ms_type:`) —
  لا يوجد أي `callback_query_handler` يستقبل هذه القيم إطلاقاً؛ ميزة غير مكتملة
  من الأساس ولا تعمل حالياً. احذفها بالكامل.
- `send_date_suggestions` — دالة جسمها `pass` فقط، لا تفعل شيئاً. احذفها.
- النظام القديم بالكامل: `sheet = spreadsheet.sheet1` وكل ما يعتمد عليه: `get_data`,
  `save_text_to_cell`, `delete_cell`, `save_file_to_cell` (ولاحظ أنها **مكرَّرة
  حرفياً مرتين** بالملف الأصلي — احذف كلا النسختين)، `merge_cell`, `get_file_ids`,
  `get_text`, `invalidate_sheet_cache`, `_sheet_cache_loop`, `_sheet_data_cache`،
  و`get_notes()` (تعتمد على النظام القديم ولا تُستدعى من أي مكان فعلي بالتدفقات
  الحالية). **السبب:** أول تبويب بالشيت الفعلي أصبح "المستخدمين" وليس بيانات
  دراسية، فأي كتابة على `sheet1` القديم كانت تخاطر بالكتابة فوق شيت المستخدمين
  بالخطأ. البيانات الحقيقية كلها الآن بالصفحات الست المخصصة (المحاضرات/الملازم/
  الملخصات/التكاليف/نماذج الاختبارات/Targets) عبر `get_tab_data`.
- عمود "السعر" و"الملاحظات" (notes) بمسار النظام القديم — الميزة الفعلية الحية
  الآن هي "سعر الملزمة" داخل صفحة "الملازم" نفسها (عمود B)، فلا حاجة لمسار سعر
  منفصل على النظام القديم المحذوف. أي تدفق `dtype == "price"` أو `dtype == "notes"`
  بمعالج الرسائل يعتمد على `sheet` القديم يُحذف معه؛ الأسعار تُدار حصراً عبر
  `save_booklet`/`replace_booklet_content` (عمود السعر بصفحة الملازم).

### 4.8 — إضافة/تعديل سلوك (وليست حذفاً — نفّذها كإضافة)
- **[10]** أضف خطوة "⚠️ تنبيه" اختيارية لتدفق "إضافة محاضرة"، بنفس تدفق "إضافة
  تكليف" تماماً: بعد اختيار الوقت يظهر صف أزرار "💾 حفظ" (يمين) و"⚠️ تنبيه"
  (يسار)، اختياري بالكامل. التنبيه يُكتب بعمود "التنبيه" (العمود الخامس) الموجود
  أصلاً بصفحة "المحاضرات" بالشيت (موجود بالهيكل لكنه لم يكن يُكتب إليه بالكود
  السابق). عدّل `save_lecture` لتقبل معامل `alert=""` اختيارياً يكتب لهذا العمود.
- **[11]** في **كل** تدفقات جمع الملفات/النصوص التالية: إضافة تكليف
  (`enter_task_content`)، إضافة ملخص (`enter_summary_content`)، تعديل محتوى موجود
  (`enter_replace_content` — مشترك بين تكليف/ملخص/ملزمة/نموذج)، طلب رفع ملف من
  مستخدم عادي (`waiting_files_req`) — **استبدل آلية المؤقت `_reset_timer` (7 ثوانٍ
  صمت) بزر "💾 حفظ" يظهر فوراً بعد أول ملف أو نص يصل**، مع بقاء إمكانية إرسال
  ملفات/نصوص إضافية قبل الضغط على الزر (الملفات تُجمَّع بقائمة `pending_files`
  كما بالأصل، فقط الزر يظهر بدل انتظار المؤقت). هذا يُسرّع الاستجابة ويُزيل حِمل
  `threading.Timer` غير الضروري لكل رسالة.
  - ملاحظة: تدفق "إضافة ملزمة" (`enter_booklet_file`) أصلاً **لا** يستخدم مؤقتاً
    (ينتظر ملفاً واحداً فقط ثم يعرض الزر مباشرة) — لا تعديل مطلوب هناك، فقط انقله
    كما هو.
- **[16]** تدفق "إضافة نموذج اختبار" (`enter_exam_file`) بالكود الأصلي يحفظ الملف
  **تلقائياً وفورياً** بدون أي زر تأكيد (`save_exam` يُستدعى مباشرة عند استلام
  الملف). **غيّر هذا** ليطابق بقية الميزات: أضف زر "💾 حفظ" يظهر بعد استلام الملف
  (بدل الحفظ الفوري)، لتوحيد سلوك كل ميزات رفع الملفات على مبدأ واحد بلا استثناء.

---

## 5) قاعدة البيانات — Google Sheets (المصدر الوحيد للبيانات)

الاتصال عبر `gspread` + `oauth2client`، بيانات الاعتماد من متغيّر بيئة
`GOOGLE_CREDENTIALS` (JSON) حصراً — **لا** فولباك لملف `credentials.json` محلي (احذف
أي فولباك من هذا النوع إن وُجد بالكود، التطوير المحلي غير مدعوم). مفتاح الشيت من
`SHEET_KEY`. **لا تفتح `spreadsheet.sheet1` إطلاقاً** (راجع القرار 4.7) — افتح كل
صفحة بالاسم العربي الصريح فقط.

### الصفحات (Tabs) وأعمدتها بالترتيب:

**`المستخدمين`** (0-indexed بالكود):
`الاسم`(0) `جهات الاتصال`(1) `ID`(2) `مسموح`(3) `ادمن`(4) `مالك`(5) `English`(6)
`لوج`(7) `bot 2`(8) `AI-ALLOWED`(9) `AUTO PUBLISH`(10) `ai_switch`(11)
`Fake AI`(12 — غير مستخدم بالكود، تجاهله). صف خاص بالاسم `"الكل"` يمثّل قيم
"open_all"/"admin_all" الافتراضية العامة لكل المستخدمين غير المسجَّلين.

**`ai_providers`**: `order` `provider` `api_key` `model` `enabled`. القيمة
`model = "auto"` تعني اختيار نموذج افتراضي حسب المزود (Gemini/OpenRouter فقط
بعد القرار 4.1).

**`المساعدة`**: `تعليمات البوت`(مفتاح) `File ID` `نوع الملف` `نوع المستخدم`
(`user`/`admin`/`all`) `نص توضيحي`. يحوي أيضاً صفاً بمفتاح `help_request_photo`
(صورة توضيحية تظهر بشاشة "غير مسموح لك").

**`القاعات والمواد`**: `المبنى` `القاعة` `الدكتور` `المادة`. يُستخدم لبناء قائمة
القاعات حسب المبنى، ولربط كل مادة باسم دكتورها (قد تتعدد الأساتذة لنفس المادة).

**`inline_buttons`**: `المفتاح` `النص العربي` `النص الإنجليزي` `اللون`
(`danger`/`success`/`primary`/فارغ) `موضع المستخدم` `موضع الأدمن` `موضع المالك`
`البطاقة` (اسم توضيحي فقط) `محتوى الأمر`. مواضع أزرار Inline **غير مستخدمة فعلياً**
بالكود (الترتيب ثابت بالكود دائماً) — تُقرأ احتياطاً فقط.

**`keyboard_buttons`**: نفس الأعمدة تقريباً بدون عمود "محتوى الأمر"، لكن **مواضعها
(E/F/G) تُستخدم فعلياً** لبناء `_keyboards_cache` بنظام شبكة (A1=عمود A صف 1، إلخ) —
راجع القسم 7 لتفاصيل هذا النظام. ⚠️ ملاحظة: بالكود الأصلي `main_menu()` **لا** تعتمد
فعلياً على هذه المواضع (تُبنى بترتيب ثابت بالكود مباشرة) — فقط `_build_keyboards_cache`
يبنيها لكن دون استدعاء فعلي واضح مستخدَم بمسار المستخدم النهائي. انقل الآلية كما هي
(بلا حذف، القرار بانتظار توضيح لاحق)، لكن لا تفترض أنها تتحكم بالقائمة الرئيسية —
`main_menu()` يبقى مبنياً بالكود الثابت كما بالأصل تماماً.

**`bot_texts`**: `المفتاح` `النص العربي` `النص الإنجليزي` فقط (بدون لون/مواضع) —
نصوص عامة (رسائل ترحيب، أخطاء، تأكيدات...). كل مفتاح بالشيت (بأي من الصفحات الثلاث
أعلاه) يُبنى منه بالذاكرة `{مفتاح}_ar` و`{مفتاح}_en`.

**`المحاضرات`**: `التاريخ` `المادة` `وقت المحاضرة` `مكان المحاضرة` `التنبيه`
(العمود الخامس — كان موجوداً بالهيكل لكن غير مُستخدَم بالكود القديم، يُفعَّل الآن
بالقرار 4.8/[10]). صف بمادة فارغة ومحتوى "لا يوجد" بعمود الوقت يعني "لا محاضرات
بهذا اليوم" (ميزة `save_no_lecture`/`date_has_lectures`).

**`الملازم`**: `المادة` `سعر الملزمة` `الملازم` (اسم/وصف الملزمة) `file_id`
`uni_message_id` `my_message_id` (آخر عمودين غير مستخدَمين فعلياً بالمنطق الحالي،
انقلهما بالهيكل فقط دون منطق إضافي).

**`الملخصات`**: `المادة` `اسم الطالب (صانع الملخص)` `الملخصات` (النص المعروض كزر)
`file_id` `uni_message_id` `my_message_id`.

**`التكاليف`**: `المادة` `اسم التكليف` `نص التكليف` `التنبيه` `file_id`
`uni_message_id` `my_message_id`.

**`نماذج الاختبارات`**: `المادة` `نوع النموذج` (الاسم) `file_id` `uni_message_id`
`my_message_id`.

**`Targets`**: `المنصة` `اسم الجهة` `ID` `الحالة` `ملاحظات` — هيكل أولي لتوسّع
مستقبلي (نشر واتساب/قنوات)، **غير مفعَّل بأي منطق حالي بالكود** — انقله كصفحة كاش
فارغة فقط (`get_tab_data("targets")`) دون أي دالة تستخدمه فعلياً، تماماً كحاله
الحالي.

---

## 6) الهيكلة المستهدفة (شجرة الملفات الإلزامية)

```
study_bot/
├── config.py                     # توكنات، أرقام أعمدة، إعدادات مركزية، بلا منطق
├── logging_utils.py               # إرسال موحّد لكل تنبيهات بوت اللوج
│
├── sheets/
│   ├── __init__.py
│   ├── connection.py               # فتح الاتصال + كل الصفحات (بلا sheet1)
│   ├── cache_utils.py               # SafeCache — سياسة الكاش الآمنة العامة
│   ├── users_repo.py                 # كل ما يخص شيت "المستخدمين"
│   ├── data_repo.py                   # كاش موحّد للصفحات الست (بدون CRUD لكل ميزة)
│   ├── texts_repo.py                   # bot_texts + keyboard_buttons + inline_buttons + bt()
│   ├── rooms_repo.py                    # شيت "القاعات والمواد" (get_rooms/doctor إن لم تكن بـ browsing)
│   └── help_repo.py                      # شيت "المساعدة" (get/save_help_material)
│
├── utils/
│   ├── __init__.py
│   ├── parsing.py                   # قراءة خلية آمنة، تواريخ، أرقام عربية
│   └── time_parsing.py               # _time12_to_24 / normalize_time / parse_time_range
│
├── ai/
│   ├── __init__.py
│   ├── providers.py                   # Gemini + OpenRouter فقط (القرار 4.1)
│   ├── assistant.py                    # ask_ai + ملخصات قاعدة البيانات للمحادثة
│   └── voice.py                         # transcribe_voice (Whisper عبر OpenRouter)
│
├── keyboards/
│   ├── __init__.py
│   ├── main_kb.py                    # القائمة الرئيسية + لوحة الإعدادات Inline
│   └── flow_kb.py                     # كل لوحات المفاتيح المرحلية
│
├── features/
│   ├── __init__.py
│   ├── browsing.py                    # get_subjects/get_rooms/get_subject_doctor...
│   ├── onboarding.py                   # /start، طلب الانضمام، مشاركة الرقم، الكود السري
│   ├── settings.py                      # سويتش مساعد نايف / النشر التلقائي / اللغة
│   ├── broadcast.py                      # notify_auto_publish + زر الإشعار + الأمر النصي
│   ├── users_admin.py                     # بطاقة المستخدم، البحث، الترقية/التخفيض
│   ├── help_materials.py                   # رفع/عرض التعليمات
│   ├── lectures.py                          # بيانات + تدفق المحاضرات (مع [10])
│   ├── assignments.py                        # بيانات + تدفق التكاليف (مع [11])
│   ├── summaries.py                           # بيانات + تدفق الملخصات (مع [11])
│   ├── booklets.py                             # بيانات + تدفق الملازم
│   ├── exams.py                                 # بيانات + تدفق نماذج الاختبارات (مع [16])
│   ├── upload_request.py                         # طلب رفع ملف (مستخدم عادي) + موافقة الأدمن
│   └── admin_commands.py                          # 3 أوامر نصية فقط (القرار 4.5)
│
├── files_io.py                     # _try_send_file / send_files_with_text
├── state.py                         # كل قواميس الحالة بالذاكرة (user_state, وغيرها)
├── http_server.py                    # _InternalHandler وكل تكامل بوت اللوج — بلا أي تعديل سلوكي
└── bot.py                             # تسجيل كل الـ handlers + ربط الميزات + run()
```

**قاعدة تسمية:** أي ملف يحتاج بيانات مستخدم متصل حالياً (لغة، سويتش AI، حالة
البث...) يقرأها مباشرة من `users_repo.get_user_record(uid)` — **لا** قواميس كاش
منفصلة موازية بالذاكرة إن أمكن الاستغناء عنها، لتقليل مصادر الحقيقة المتعددة وتسريع
القراءة (نفس مبدأ طبّقناه بالفعل على `bt()`/`main_menu()`/`settings_inline_menu()`).
استثناء: `_ai_histories` (سياق محادثة AI لكل مستخدم) و`user_state` (حالة التدفق
الحالي) تبقيان قواميس ذاكرة منفصلة بالضرورة — تُنقلان إلى `ai/assistant.py` و
`state.py` على التوالي.

---

## 7) نظام النصوص والأزرار — تفصيل دقيق (لتجنّب أي التباس)

- `bt(key, uid=None)`: يرجع نص بلغة المستخدم (`{key}_ar` أو `{key}_en`)، مع تراجع
  (fallback) تلقائي للعربي إن كانت الترجمة الإنجليزية فارغة، ثم للقيمة الافتراضية
  الثابتة بالكود (`DEFAULT_BOT_TEXTS`) إن لم يوجد شيء بالشيت إطلاقاً.
- كل صف بأي من الصفحات الثلاث (`bot_texts`, `keyboard_buttons`, `inline_buttons`)
  يُدمَج بقاموس واحد `BOT_TEXTS` بالذاكرة عند `load_bot_texts()`. صفوف بلا نص عربي
  ولا إنجليزي (عناوين أقسام/ملاحظات تنظيمية بالشيت) تُتجاهل.
- الألوان (`danger`/`success`/`primary`) تُقرأ من العمود D بأي من صفحتي الأزرار،
  وتُطبَّق عبر `_make_btn`/`_make_inline` مع سقوط آمن (try/except) إن كانت نسخة
  المكتبة لا تدعم معامل `style` (توافق Telegram Bot API 9.4+).
- `BUTTON_TEXTS`: مجموعة (set) لكل نصوص الأزرار المسموحة (من الشيت + ثابتة بالكود)،
  تُستخدم بمعالج الرسائل لتمييز "ضغطة زر" عن "نص حر" (رسالة عادية أو سؤال AI). يجب
  نقلها بكل عناصرها الثابتة بالكود كما هي (قائمة الأزرار الداخلية الثابتة مثل
  "↩️ رجوع خطوة"، "✅ إرسال"، وغيرها المذكورة صراحة بالكود الأصلي).
- **حساسية الترتيب:** فحص "هل النص زر معروف؟" يجب أن يسبق أي محاولة لتفسيره كسؤال
  موجّه لمساعد الذكاء الاصطناعي — هذا الترتيب موجود بالأصل ويجب الحفاظ عليه حرفياً
  (راجع الشرط `if text in BUTTON_TEXTS` قبل شرط تفعيل الـ AI بدالة `handle_message`).

---

## 8) سياسة الكاش (SafeCache) — إلزامية لكل صفحة شيت

كل صفحة تُقرأ عبر كائن كاش واحد بالنمط التالي (طبّقناه فعلاً بـ `sheets/cache_utils.py`
سابقاً — استخدم نفس النمط):

```python
class SafeCache:
    def __init__(self, name): self.name, self.data, self.ts = name, None, 0.0
    def get_or_fetch(self, fetch_fn):
        if self.data is not None: return self.data
        return self.refresh(fetch_fn)
    def refresh(self, fetch_fn):
        try:
            fresh = fetch_fn()
        except Exception as e:
            log_error(f"⚠️ فشل تحديث كاش «{self.name}» — تم الاحتفاظ بآخر بيانات معروفة. السبب: {e}")
            return self.data
        self.data = fresh
        self.ts = time.time()
        return self.data
```

**القاعدة الذهبية:** لا يُستبدل `self.data` أبداً إلا بعد نجاح الجلب الكامل. عند أي
استثناء، القيمة القديمة تبقى كما هي ويُرسل تنبيه لبوت اللوج فوراً. **ممنوع تفريغ
الكاش (`= None`) عند الفشل مهما كان السبب.**

**متى يُحدَّث الكاش:**
1. بعد أي عملية `append_row`/`update_cell`/`delete_rows` ناجحة من داخل البوت
   (استدعِ `refresh_data_cache("اسم_الصفحة")` مباشرة بعد نجاح الكتابة).
2. عند أمر يدوي من بوت اللوج عبر `_InternalHandler` (`/refresh_texts`,
   `/refresh_users`, `/refresh_ai`, `/refresh_data`, `/refresh_all`) — هذا الجزء
   (endpoint HTTP الداخلي على `INTERNAL_PORT`) **يبقى بلا أي تعديل سلوكي**، فقط
   انقله لملف `http_server.py` واجعله يستدعي الدوال الجديدة (`refresh_users_cache`,
   `refresh_data_cache`, `load_bot_texts`, `load_ai_providers`) بدل الأسماء القديمة.

---

## 9) التكامل مع بوت اللوج (يبقى كما هو حرفياً)

- `LOG_BOT_TOKEN`: توكن بوت لوج منفصل، يُستخدم فقط لإرسال رسائل نصية (لا يُشغَّل
  polling له من هذا الملف).
- endpoint HTTP داخلي (`http.server.HTTPServer` على `INTERNAL_PORT`، افتراضي 10001)
  يستقبل أوامر POST بحقل `secret` (يُقارَن بـ `INTERNAL_SECRET`) و`cmd`
  (`refresh_texts`/`refresh_users`/`refresh_ai`/`refresh_data`/`refresh_all`/`status`).
- `get_log_user_ids()`: قائمة مستخدمين بعمود "لوج"=TRUE بشيت المستخدمين، تُحمَّل مرة
  واحدة عند التشغيل (`_load_log_users`)، وتُحدَّث فقط عبر `/refresh_log_users` (خارج
  بوت الدراسة نفسه — لا تلمسه).
- انقل هذا القسم بالكامل لملف `http_server.py` مستقل، مع استيراد الدوال المطلوبة
  من الملفات الجديدة بدل استدعاء دوال عالمية بملف واحد كما بالأصل.

---

## 10) تفصيل كل ميزة — تأكيد عدم النقص (Checklist إلزامي)

انسخ هذه القائمة واستخدمها كقائمة تحقّق (checklist) قبل تسليم النتيجة — كل بند يجب
أن يكون موجوداً بمكان واحد بالهيكل الجديد ما لم يكن مذكوراً بقسم 4 كمحذوف:

### أ) الانضمام والصلاحيات
- [ ] `/start` بكل حالاته: مستخدم مسموح، غير مسموح (بدون هاتف مسجَّل → عرض صورة/
      رسالة + زرين "مشاركة رقمي"/"لا أريد")، غير مسموح (هاتف مسجَّل مسبقاً → طلب
      تلقائي للمالك بلا سؤال جديد)، `/start refresh` (تحديث كاش، خاص بالمالك)،
      `/start show_user_<id>` (رابط عميق لبطاقة مستخدم).
- [ ] تدفق مشاركة جهة الاتصال الكامل (أزرار Inline + كيبورد فعلي لمشاركة الرقم +
      حذف الكيبورد المؤقت عند الرجوع) — `handle_request_contact_confirm`,
      `handle_request_contact_no`, `handle_request_contact_back`, `handle_contact`.
- [ ] تسجيل الزائر تلقائياً بعلامة 🆕️ عند أي `/start` (`_register_new_visitor`)
      بغض النظر عن نتيجة الصلاحية.
- [ ] الكود السري الرقمي للتفعيل اليدوي (`calc_secret_code` = مجموع أرقام الـ ID
      + يوم الشهر الحالي بتوقيت اليمن).
- [ ] بطاقة طلب انضمام جديد للمالك (6 أزرار: أدمن/مستخدم/تغيير اسم/تفعيل AI/رفض)
      وكل الـ callbacks (`approve_role_admin_*`, `approve_role_user_*`,
      `approve_rename_*`, `approve_ai_on_*`, `reject_*`) + حالة
      `awaiting_rename_for_approval`.
- [ ] `notify_owners_new_request`, `notify_owners_action` (بمنطق منع التكرار
      بمهلة `_NOTIFICATION_COOLDOWN`).

### ب) الإعدادات (`⚙️ الإعدادات`)
- [ ] لوحة Inline: سويتش مساعد نايف (يظهر فقط إن وُجد مزود AI نشط)، سويتش النشر
      التلقائي، اختيار اللغة (عربي/إنجليزي) مع علامة ✅ بجانب المُختار حالياً.
- [ ] طلب صلاحية AI من المالك (`ai_request_yes`/`ai_request_no` للمستخدم،
      `grant_ai_*`/`deny_ai_*` للمالك).
- [ ] حفظ حالة السويتشات بالشيت (`ai_switch`, `AUTO PUBLISH`, `English`) فوراً
      عند التبديل، مع تحديث فوري بالذاكرة أيضاً.

### ج) التصفح العام (مستخدم عادي وأدمن/مالك)
- [ ] `📚 المواد` ← اختيار مادة ← قائمة خيارات (🕐 الجدول / 📝 التكاليف /
      📖 الملخص / 📋 الملزمة / 🧾 نماذج الاختبارات) ← عرض حسب الخيار.
- [ ] `🕐 أوقات المحاضرات` (اختصار آخر يوم دراسي، بتنسيق عربي كامل: اسم اليوم،
      التاريخ بصيغة yyyy/mm/dd، ترقيم "المحاضرة الأولى/الثانية..."، تحويل الوقت
      لصيغة 12 ساعة عربية ص/م، اسم الدكتور إن وُجد).
- [ ] `📝 التكاليف`, `📖 الملخصات`, `📋 الملازم`, `🧾 نماذج الاختبارات` (اختصارات:
      اختيار مادة إن تعددت، أو عرض مباشر إن كانت مادة واحدة فقط لديها بيانات).
- [ ] عرض محتوى كل عنصر (نص + ملف واحد أو أكثر) عبر `send_files_with_text`
      (caption واحد إن كان ملف واحد، رسالة نص منفصلة إن كانت عدة ملفات).

### د) إضافة بيانات (أدمن/مالك) — `➕ إضافة`
- [ ] إضافة محاضرة: تاريخ (باقتراحات تلقائية غد/اليوم/أمس) ← مبنى (مع "لا يوجد"
      إن لم يكن هناك محاضرات مسجّلة بهذا التاريخ بعد → يسجّل "لا يوجد محاضرات") ←
      قاعة (من شيت القاعات) ← مادة ← وقت (أزرار جاهزة + "⏰ توقيت آخر" حر) ← كشف
      تعارض وقت مع محاضرة أخرى بنفس اليوم (`check_lecture_conflict`، خيار
      "🔄 استبدال") ← **[جديد 4.8] خطوة تنبيه اختيارية** ← حفظ + إشعار نشر تلقائي.
- [ ] إضافة تكليف: مادة ← اسم ← نص و/أو ملفات (**[11] زر حفظ فوري**) ← تنبيه
      اختياري ← حفظ.
- [ ] إضافة ملخص: مادة (من كل مواد صفحة المحاضرات، بلا استثناء) ← ملف/ملفات
      (**[11] زر حفظ فوري** يقود لاختيار الطالب) ← اختيار اسم الطالب من قائمة
      معروفة أو "➕ إضافة طالب" جديد ← حفظ.
- [ ] إضافة ملزمة: مادة ← اسم ← ملف واحد ← زر "💾 حفظ" أو "💰 إضافة سعر" (اختياري).
- [ ] إضافة نموذج اختبار: مادة (من صفحة المحاضرات) ← اسم/نوع النموذج ← ملف ←
      **[16] زر حفظ** (بدل الحفظ الفوري بالأصل).
- [ ] رفع تعليمات (`📹 رفع التعليمات`، أدمن فقط): جمهور (مستخدمين/أدمن) ← نص
      توضيحي اختياري ← ملف/ملفات.
- [ ] زر "↩️ رجوع خطوة" يعمل بدقة بكل خطوة من كل تدفق أعلاه (راجع الكود الأصلي —
      منطق مفصّل جداً لكل حالة `step`، انقله بالكامل بلا اختصار).

### هـ) تعديل/حذف بيانات — `✏️ تعديل`
- [ ] لكل نوع (محاضرة/تكليف/ملخص/ملزمة/نموذج): اختيار مادة ← اختيار العنصر
      المحدد (تاريخ لمحاضرة، أو اسم للباقي) ← عرض القيمة الحالية ← زرين
      "✏️ تعديل" (يفتح `enter_replace_content` **بزر حفظ [11]** بدل مؤقت) أو
      "🗑 حذف" (تأكيد صريح "✅ نعم، احذف").
- [ ] حالة خاصة لتعديل وقت محاضرة فقط (`save_lecture_time`) منفصلة عن حذف
      المحاضرة الكاملة.

### و) الإشعارات
- [ ] `📢 إرسال إشعار` (أدمن/مالك): نص اختياري ← ملف اختياري ← إرسال لكل
      المستخدمين المسجَّلين (أو المصرَّح لهم فقط إن لم يكن الوضع "مفتوح للجميع").
- [ ] `notify_auto_publish`: إشعار تلقائي للمشتركين بـ"النشر التلقائي" عند أي
      إضافة جديدة (محاضرة/تكليف/ملخص/ملزمة/نموذج) — استدعِها من كل دالة `save_*`
      المعنية بعد نجاح الكتابة.

### ز) إدارة المستخدمين (مالك فقط) — `👥 إدارة المستخدمين`
- [ ] بحث موحّد ذكي (`_smart_search_user`): يقبل ID، رقم هاتف (جزئي مع/بدون كود
      دولة)، أو جزء من الاسم (بدعم `#اسم_مستخدم`) بنفس الاستعلام.
- [ ] عرض جميع المستخدمين (`format_all_users_message`، مقسّم لـ"نشطون" و"زوار
      جدد 🆕️")، آخر 3 مستخدمين.
- [ ] بطاقة المستخدم (`send_user_card`): منطق الزرين الذكي (⭐ أدمن / 👤 مستخدم)
      الذي يتغيّر نصه ووظيفته حسب الحالة الحالية للمستخدم (ترقية/تخفيض/إقفال/إلغاء
      — 6 سيناريوهات مختلفة بالضبط كما بالكود الأصلي)، + تفعيل/تعطيل AI + تغيير
      اسم، مع تحديث الرسالة بالمكان (edit) بدل إرسال رسالة جديدة إن كانت موجودة.
- [ ] `update_user_card_in_chat` — تحديث تلقائي للبطاقة المفتوحة عند أي تغيير رتبة
      يحدث من مصدر آخر (أمر نصي، أو تعديل مباشر بالشيت عبر `_watch_sheet_loop`).

### ح) نظام الذكاء الاصطناعي "مساعد نايف"
- [ ] يعمل **فقط** في الصفحة الرئيسية (state فارغ تماماً) — أي دخول لأي تدفق آخر
      يُعطّل استقبال الأسئلة حتى العودة للرئيسية (السويتش يبقى مفعَّلاً بالذاكرة/
      الشيت، فقط لا يُستهلَك بمنتصف تدفق آخر).
  - [ ] الشروط الأربعة الإلزامية بالترتيب: يوجد مزود AI نشط ← المستخدم لديه
      `ai_allowed=TRUE` ← السويتش الشخصي `ai_switch=TRUE` ← `state` فارغ.
- [ ] رسالة "✍️ نايف يكتب ..." متحركة (نقاط متزايدة كل 0.3 ثانية) بخيط منفصل،
      تُحذف عند وصول الرد.
- [ ] دعم التسجيلات الصوتية (`transcribe_voice` عبر Whisper بواسطة OpenRouter) —
      يعمل فقط بنفس شروط تفعيل الـ AI، وإلا يتجاهل الصوت بصمت تام (بلا رسالة خطأ).
- [ ] سياق محادثة لكل مستخدم (`_ai_histories`, حد أقصى 20 رسالة) يُرسَل مع كل
      طلب لـ OpenRouter تحديداً (Gemini لا يستخدم سياقاً متعدد الأدوار بنفس الطريقة
      بالكود الأصلي — راجع `call_gemini` مقابل `call_openrouter`).
- [ ] `get_data_summary_for_ai`: ملخص ديناميكي كامل لقاعدة البيانات (كل مادة:
      محاضراتها/تكاليفها/ملخصاتها/ملازمها/نماذجها) + معلومات المستخدم الشخصية +
      (للمالك فقط) إحصائيات عامة عن المستخدمين.
- [ ] `get_bot_code_summary`: دليل استخدام ديناميكي يُبنى من `bt()` الحية (أسماء
      الأزرار الفعلية من الشيت) + المواد الحقيقية — **لا** نصوص ثابتة بالكود تصف
      أزراراً قد لا تُطابق تسمية الشيت الفعلية.
- [ ] رسائل الفشل: `رسالة_ai_غير_مفعل` (لا يوجد مزود)، `رسالة_ai_غير_مسموح`
      (لا صلاحية)، `رسالة_ai_فشل` (فشل الاتصال بكل المزودين بالترتيب).

### ط) طلب رفع ملف (مستخدم عادي بلا صلاحية أدمن)
- [ ] `📨 طلب رفع ملف`: مادة ← نوع (تكليف/ملخص) ← تاريخ ← ملف/ملفات
      (**[11] زر حفظ يقود لـ"✅ إرسال"** — لاحظ أن هذا التدفق أصلاً ينتهي بزر
      "✅ إرسال" صريح، تحقّق فعلياً هل مؤقت `_reset_timer` مستخدَم هنا بالكود
      الأصلي أم لا قبل التعديل — إن لم يوجد مؤقت أصلاً بهذا المسار الخاص، لا حاجة
      لأي تغيير، فقط انقله كما هو).
- [ ] إرسال الطلب لكل الأدمن/المالك مع أزرار قبول/رفض (`file_req:approve:*`,
      `file_req:reject:*`)، وحفظ الملف مباشرة بالعمود المناسب عند القبول.

### ي) اللوج والمراقبة
- [ ] `tg_log`/`log_info`/`log_warning`/`log_error`/`log_critical` — رسالة موحّدة
      لكل مستويات اللوج، مع بيانات المستخدم (اسم/هاتف/أيقونة رتبة) إن أُرسل `uid`.
- [ ] `_watch_sheet_loop` (كل 30 ثانية، استثناء مصرَّح به — راجع قاعدة 4 بالقسم 3):
      يكتشف كل تغييرات الرتبة/AI/الصلاحية التي تحدث **مباشرة على الشيت من خارج
      البوت** (وليس عبر أزراره)، ويُرسل إشعاراً للمستخدم المتأثر ولبوت اللوج.
- [ ] `_do_full_refresh` + `_check_bot_texts` (فحص اكتمال كل مفاتيح `bot_texts`
      وإرسال تقرير نقص لبوت اللوج) — يبقيان بلا تغيير سلوكي.

---

## 11) نقاط تحتاج قراراً منك أثناء التنفيذ (اجعلها افتراضات معقولة واذكرها بالتقرير النهائي، لا تتوقف بسببها)

- تدفق `waiting_files_req` (طلب رفع ملف): تحقّق فعلياً من الكود الأصلي هل يستخدم
  `_reset_timer` أم ينتظر ضغط "✅ إرسال" الصريح مباشرة — طبّق القرار 4.8/[11] فقط
  إن وجدت مؤقتاً فعلياً هناك.
- `_keyboards_cache`/`_build_keyboards_cache` (نظام مواضع شبكة A1/B2 لأزرار
  الكيبورد): انقلها بالكامل كما هي رغم أنها لا تبدو مستخدمة فعلياً بمسار
  `main_menu()` الحي — لا تحذفها، فقط لا تفترض أنها تتحكم بأي قائمة ظاهرة للمستخدم
  حالياً ما لم يثبت العكس من الكود.
- إن وجدت أي دالة أو مسار غير مذكور صراحة بالقسم 10 أو بقرارات الحذف بالقسم 4،
  **انقله كما هو افتراضياً** ولا تحذفه، واذكره بالتقرير النهائي كنقطة تحتاج تأكيداً.

---

## 12) معيار القبول النهائي

النتيجة مقبولة فقط إذا:
1. كل بند بقائمة التحقق بالقسم 10 موجود وفعّال بمكان واحد بالهيكل الجديد.
2. كل قرار حذف بالقسم 4 (4.1 حتى 4.7) منفَّذ بالكامل ولا أثر لأي شيفرة محذوفة.
3. كل إضافة/تعديل بالقسم 4.8 (10، 11، 16) منفَّذة وتعمل بنفس نمط الأزرار المذكور.
4. سياسة الكاش بالقسم 8 مطبَّقة حرفياً على كل صفحة شيت بلا استثناء.
5. البوت يشتغل فعلياً بـ `python -m study_bot.bot` أو ما يعادلها، ويحافظ على كل
   الأزرار والرسائل والتدفقات بنفس النصوص العربية الأصلية دون أي ترجمة أو تبسيط.
